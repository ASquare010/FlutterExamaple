import 'package:flutter/material.dart';

class FlameGame extends StatefulWidget {
  const FlameGame({Key key}) : super(key: key);

  @override
  _FlameGameState createState() => _FlameGameState();
}

class _FlameGameState extends State<FlameGame> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
